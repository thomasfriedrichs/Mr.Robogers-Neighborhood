function returnNumbers(usernumber) {
  let numbers = []
  for (let i = 0; i <= usernumber; i ++) {
    numbers.push(i)
    if (numbers[i].uncludes(3)) {
      i[33] = "Won't you be my neighbor?"
    }
  }  
  return numbers
}
returnNumbers(50);